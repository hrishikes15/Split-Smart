"use client";

import { CinematicHero } from "@/components/ui/cinematic-hero";

const Landing = () => {
    return (
        <div className="relative min-h-screen bg-transparent">
            <CinematicHero className="bg-transparent" />
        </div>
    );
};

export default Landing;
